<%- include ../partials/header %>  <%- include ../partials/categories-navbar %>
<div class="container">
  <!-- Flash messages -->
  <div class="row mt-3 mb-3">
    <div class="col-md-12 m-auto">
      <% if (successMsg) { %>
      <div id="success" class="alert alert-success">
        <%= successMsg %>
      </div>
      <% } else { %>
      <div id="success" class="alert alert-success d-none"></div>
      <% } %>
    </div>
    <div class="col-md-12 m-auto">
      <% if (errorMsg) { %>
      <div id="error" class="alert alert-danger">
        <%= errorMsg %>
      </div>
      <% } else { %>
      <div id="error" class="alert alert-danger d-none"></div>
      <% } %>
    </div>
  </div>
  <!-- cachedresponse info -->
  <div class="row mt-4 mb-4">
    <div class="col-md-5 mt-2 mr-2 ml-auto">
      <img
        src="<%=cachedresponse.imagePath%>"
        class="img-fluid cachedresponse-img"
        alt="cachedresponse image"
      />
    </div>
    <div class="col-md-5 mt-2 mb-2 ml-2 mr-auto text-dark">
      <h2><%=cachedresponse.title%></h2>
      <hr />
      <p class="price">USD $<%=cachedresponse.price%></p>
      <p class="lead"> Description: 
        <p><%=cachedresponse.description%></p>
      </p>
      
      <p><strong>Category:</strong> <%=cachedresponse.category.title%></p>
      <p><strong>Brand: </strong><%=cachedresponse.manufacturer%></p>
      <p><strong>cachedresponse code:</strong> <%=cachedresponse.cachedresponseCode%></p>
      <p><strong>Date added to the store:</strong> <%= moment(cachedresponse.createdAt).format( 'MMMM Do YYYY')%></p>
      <% if(cachedresponse.available) {%>
      <p class="is-available text-center">In stock</p>
      <a href="/add-to-cart/<%=cachedresponse._id%>" class="btn btn-block btn-secondary mt-3 mb-3 button-style">
        Add to Shopping Cart
      </a>
      <% } else { %>
      <p class="is-not-available text-center">Sold out</p>
      <%} %>
    </div>
  </div>
</div>
<%- include ../partials/footer %>
